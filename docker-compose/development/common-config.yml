services:
  network-deploy-service:
    networks:
      - vocasia

  base-config:
    extends:
      service: network-deploy-service
    deploy:
      resources:
        limits:
          memory: 700m